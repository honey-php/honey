<?php

/**
 * Place for custom, user code
 *
 * If you want to extend the functionality, use "custom.php"
 * as a bootstrap file and "custom" directory as a place
 * for additional classes, libraries, etc.
 *
 * Require your custom files here. Use CUSTOM_PATH constant.
 *
 * Example:
 $ require CUSTOM_PATH . '/myclasses/MyClass.php';
 */

/**
 * Available hooks:
 * database.connection.before, database.connection.added( $name, $connection ), database.connection.after
 * ready( $app )
 */
